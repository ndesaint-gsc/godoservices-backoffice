import Placeholder from '@/components/common/Placeholder';

const SuscDigitales = () => (
  <Placeholder
    title="Suscripciones digitales"
    note="El backoffice antiguo sirve esta sección como fragmento HTML (no JSON). Requiere un endpoint JSON para migrarla a React."
  />
);

export default SuscDigitales;
