import Placeholder from '@/components/common/Placeholder';

const SuscTienda = () => (
  <Placeholder
    title="Tienda (Apple/Google)"
    note="Las suscripciones de Apple/Google se sirven como fragmento HTML en el backoffice antiguo (no JSON). Requiere un endpoint JSON para migrarlas a React."
  />
);

export default SuscTienda;
