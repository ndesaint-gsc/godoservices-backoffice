import Placeholder from '@/components/common/Placeholder';

const SuscCrear = () => (
  <Placeholder
    title="Crear suscripción telemática"
    note="El envío de oferta (send-offer) es JSON, pero el catálogo de productos y planes de pago se sirve como HTML. El formulario se añadirá cuando el catálogo esté disponible como JSON."
  />
);

export default SuscCrear;
