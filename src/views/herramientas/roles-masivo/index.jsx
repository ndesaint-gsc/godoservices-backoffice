import Placeholder from '@/components/common/Placeholder';

const HtRolesMasivo = () => (
  <Placeholder
    title="Asignación masiva de roles"
    note="El formulario (CSV + fechas + email) es migrable, pero el selector de roles asignables se sirve como catálogo HTML en el backoffice antiguo. Se añadirá cuando el catálogo esté disponible como JSON."
  />
);

export default HtRolesMasivo;
