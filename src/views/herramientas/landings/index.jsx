import Placeholder from '@/components/common/Placeholder';

const HtLandings = () => (
  <Placeholder
    title="Landings"
    note="El listado de landings se sirve como fragmento HTML en el backoffice antiguo (no JSON). Requiere un endpoint JSON para migrarlo a React."
  />
);

export default HtLandings;
